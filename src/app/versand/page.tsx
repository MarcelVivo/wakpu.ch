import type { Metadata } from 'next';
import Link from 'next/link';
import { getSiteSettings } from '@/lib/catalog';
import { formatCHF } from '@/lib/status';
export const metadata:Metadata={title:'Versand',alternates:{canonical:'https://wakpu.ch/versand'}};
export default async function Page(){const s=await getSiteSettings();return <main id="main-content" className="page-shell prose"><p className="eyebrow">BIS ZU DEINER TÜR</p><h1>Versand. Transparent.</h1><h2>Liefergebiet</h2><p>Bestellungen sind zunächst nur mit Lieferadresse in der Schweiz vorgesehen.</p><h2>Lieferzeit</h2><p>{s.shipping_text}</p><h2>Versandkosten</h2><p>{s.shipping_cost_cents===null?'Die Versandkosten werden vor dem Verkaufsstart bekannt gegeben.':`Versand pro Bestellung: ${formatCHF(s.shipping_cost_cents)}. Der Gesamtbetrag wird im Warenkorb und vor der Zahlung angezeigt.`}</p><h2>Versandursprung</h2><p>{s.shipping_origin||'Der endgültige Lieferant und Versandursprung stehen noch nicht fest. Die Angaben werden vor dem Verkaufsstart ergänzt.'}</p><h2>Sendungsverfolgung</h2><p>Sobald die Sendungsinformationen vorliegen, erhältst du eine E-Mail. Über deinen persönlichen Bestelllink kannst du den Status abrufen.</p><h2>Beschädigt angekommen?</h2><p>Bitte kontaktiere uns mit deiner Bestellnummer und einer Beschreibung des Schadens. Wir klären die nächsten Schritte direkt mit dir.</p><Link className="button button-dark" href="/kontakt">Zum Kontakt</Link></main>;}
