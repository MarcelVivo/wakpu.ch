import { login } from '../actions';
export default async function LoginPage({searchParams}:{searchParams:Promise<{status?:string}>}){
  const {status}=await searchParams;const configured=Boolean(process.env.ADMIN_EMAIL&&process.env.NEXT_PUBLIC_SUPABASE_URL&&process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);
  return <div className="admin-login"><p className="eyebrow">WAKPU INTERN</p><h1>Anmelden.</h1>{!configured?<p>Der Admin-Zugang ist noch nicht eingerichtet. Konfiguriere Supabase und ADMIN_EMAIL gemäss README.</p>:<><p>Nur für freigeschaltete Administrationskonten.</p>{status==='error'&&<p role="alert" className="admin-notice is-error">Die Anmeldung ist fehlgeschlagen. Bitte prüfe deine Zugangsdaten.</p>}<form action={login} className="admin-form"><label>E-Mail<input type="email" name="email" autoComplete="username" required /></label><label>Passwort<input type="password" name="password" autoComplete="current-password" required /></label><button type="submit">Anmelden</button></form></>}</div>;
}
