import type { Metadata } from 'next';
import { getSiteSettings } from '@/lib/catalog';
export const metadata:Metadata={title:'Impressum',alternates:{canonical:'https://wakpu.ch/impressum'}};
export default async function Page(){const s=await getSiteSettings();return <main id="main-content" className="page-shell prose"><p className="eyebrow">KLARE INFORMATIONEN</p><h1>Impressum.</h1>{!s.company_name&&<p className="panel">Konfigurationsentwurf: Die Angaben zur verantwortlichen Person oder Firma werden vor dem Verkaufsstart vervollständigt.</p>}<h2>Verantwortlich für wakpu.ch</h2><p>{s.company_name||'[NAME / FIRMA]'}<br/>{s.company_address||'[ADRESSE]'}<br/>{s.company_postcode_city||'[PLZ ORT]'}</p><h2>Kontakt</h2>{s.support_email?<a href={`mailto:${s.support_email}`}>{s.support_email}</a>:<p>[E-MAIL]</p>}<h2>Produktdarstellung</h2><p>Die aktuell verwendeten Bilder sind Produktvisualisierungen. Die tatsächlich gelieferte Farbe wird bei Mystery Color zufällig ausgewählt. Verbindliche Produktangaben werden vor dem Verkaufsstart ergänzt.</p></main>;}
